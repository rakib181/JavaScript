//When we create a function using new Function(), 
// that function is independent of its parent scope. 
// It is essentially created in a global execution 
// context, not inheriting any strict mode 
//settings from the surrounding environment.